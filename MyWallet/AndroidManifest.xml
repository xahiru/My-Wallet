<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="day12.jan.y2012"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="15" />

    <application
        android:name=".MyWalletApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity android:name=".PrefsActivity" />
        <activity android:name=".AccountsActivity" />
        <activity android:name=".AddNewAccountActivity" />
        <activity android:name=".CurrencyConverterActiviy" />
        <activity
            android:name=".MyWalletActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>